Place `mascot_source.png` here to replace the fallback sprite, then run:

```bash
python3 assets/convert_mascot.py
```

The converter writes `include/assets/mascot_base.hpp` as a 112x176 RGB565 image.
